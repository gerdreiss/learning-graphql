type Query {
    shows(titleFilter: String): [Show]
    show(id: String!): Show
}

type Show {
    id: UUID
    title: String
    releaseYear: Int
    cast: [Actor]
}

type Actor {
    name: String
    born: Date
}

scalar UUID
scalar Date
